on:
  workflow_call:

jobs:
  bump-version:
    name: Bump Patch Version
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Branch
        uses: actions/checkout@v4 

      - name: Checkout Addtional Scripts
        uses: actions/checkout@v4
        with:
          repository: TravelSizedLions/actions

      - name: Cache dasel
        id: cache-dasel
        uses: actions/cache@v4
        env:
          cache-name: dasel-install
        with:
          path: /usr/bin/dasel
          key: dasel-cache-key

      - name: Install dasel
        if: ${{ steps.cache-dasel.outputs.cache-hit != 'true' }}
        run: |
          sudo apt-get install -y dasel
          which dasel

      - name: Bump Patch Version
        id: bump
        run: |
          chmod +x ./scripts/bump-patch.sh
          ./scripts/bump-patch.sh ${{ github.event.repository.name }}

      - name: Tag & Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "v${{ steps.bump.outputs.next_version }}"
          tagging_message: "v${{ steps.bump.outputs.next_version }}"
